# Pinned toolchain versions. Read by asdf and mise; mirrored by
# rust-toolchain.toml (Rust) and .moon/toolchains.yml (which pins Bun as the
# toolchain *Moon* provisions for tasks — this file pins the `moon` CLI binary
# itself, which is a separate concern: something has to exist on PATH before
# any `moon` command can run at all. CI reads this line to install it; see
# .github/workflows/ci.yml and release.yml.
#
# Bun is the ONLY JavaScript runtime and package manager in this repository.
# Node.js is deliberately absent — do not add it.
bun 1.4.2
rust 1.98.0
moon 2.5.4
