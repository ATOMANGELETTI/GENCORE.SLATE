# Pinned toolchain versions. Read by asdf and mise; mirrored by
# rust-toolchain.toml (Rust) and .moon/toolchains.yml (Moon).
#
# Bun is the ONLY JavaScript runtime and package manager in this repository.
# Node.js is deliberately absent — do not add it.
bun 1.4.2
rust 1.98.0
