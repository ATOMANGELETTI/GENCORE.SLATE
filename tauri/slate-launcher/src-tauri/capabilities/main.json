{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "main",
	"description": "The least this window needs: read its own state, listen for its own events, and call the suite's own commands. No filesystem, shell, network, or process permission is granted — every one of those would have to be justified by a specific feature (.agents/rules/08-tauri-and-security.md).",
	"windows": ["main"],
	"permissions": [
		"core:app:default",
		"core:event:default",
		"core:window:default",
		"core:webview:default"
	]
}
