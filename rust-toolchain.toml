# Pins the exact Rust toolchain for every developer, agent, and CI runner.
# rustup reads this automatically; do not invoke `rustup default`.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy", "rust-src", "llvm-tools"]
targets = ["x86_64-pc-windows-msvc"]
profile = "minimal"
