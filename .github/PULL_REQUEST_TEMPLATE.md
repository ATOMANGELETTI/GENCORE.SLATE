## What changed

<!-- One or two sentences. The diff shows what; explain why. -->

## How it was verified

<!-- What you actually ran, not what should work. -->

- [ ] `bun run check` passes
- [ ] Tests added or updated for the behaviour that changed
- [ ] Screenshots below, in **both** themes (UI changes only)
- [ ] Portability verified per `.agents/workflows/debug-portability.md`
      (changes to paths, packaging, or process spawning)
- [ ] `bun run agents:sync` run and the generated files committed
      (changes under `.agents/`)
- [ ] A changeset is included, or the change has no user-visible effect

## Notes for the reviewer

<!-- Anything surprising, any trade-off made, anything you are unsure about. -->
